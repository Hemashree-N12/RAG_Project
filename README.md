# RAG-Based Customer Support Assistant with LangGraph & HITL

## 📖 Overview
This project implements a **Retrieval-Augmented Generation (RAG)** based Customer Support Assistant.  
It combines:
- 📥 PDF ingestion for knowledge base creation  
- 🧠 HuggingFace embeddings stored in ChromaDB  
- ⚡ Groq LLM for contextual answers  
- 🔄 LangGraph for workflow orchestration  
- 👩‍💻 Human-in-the-Loop (HITL) for low-confidence queries  
- 🌐 CLI + Streamlit UI interfaces  

The goal is to provide **factual, reliable, and scalable customer support automation**.

---

## 🏗️ Architecture
Check out output_screenshot for system structure.
*Figure 1: System architecture showing ingestion, retrieval, LLM, workflow, and HITL.*

---

## 📂 Project Structure
```
├── ingest.py        # PDF ingestion and embedding
├── query.py         # Retrieval from ChromaDB
├── llm_groq.py      # Groq LLM integration
├── workflow.py      # LangGraph orchestration
├── hitl.py          # Human-in-the-Loop escalation
├── app.py           # Streamlit UI
├── main.py          # CLI entry point
├── README.md        # Project overview
├── HLD.pdf          # High-Level Design
├── LLD.pdf          # Low-Level Design
├── Technical_Documentation.pdf # Detailed documentation
└── OutPut-Screenshots/ # Screenshots folder
```

---

## ⚙️ Setup
```bash
conda create -n rag_env python=3.10
conda activate rag_env
pip install -r requirements.txt
```

---

## 📥 Data Ingestion
Run:
```bash
python ingest.py
```
Expected output:  
`Loaded chunks: 103`  
`Embeddings stored in ChromaDB`

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`  
*Figure 2: PDF ingestion and embedding creation.*

---

## 🔎 Query Retrieval
Run:
```bash
python query.py
```
**Demo Question:**  
`What is the SLA for Priority 1 incidents?`  

Expected output: Retrieved chunks showing SLA details.

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`  
*Figure 3: Retrieval answering SLA query.*

---

## 🤖 LLM Integration (Groq)
Run:
```bash
python llm_groq.py
```
**Demo Question:**  
`Explain the escalation process for Priority 2 incidents.`  

Expected output: Contextual answer generated by Groq LLM.  
*(Currently placeholder if Groq API not running.)*

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`

---

## 🔄 Workflow Orchestration
Run:
```bash
python workflow.py
```
**Demo Question:**  
`What are the support hours for US customers?`  

Expected output: Routed through LangGraph nodes → Answer Node → Output Node.  
*(Currently placeholder if workflow errors occur.)*

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`

---

## 🧑‍💻 Human-in-the-Loop (HITL)
**Demo Question:**  
`What is the refund policy for cancelled tickets?`  

Expected output: Confidence < 0.7 → Escalation to HITL.  
*(Currently placeholder.)*

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`

---

## 🌐 Streamlit UI
Run:
```bash
streamlit run app.py
```
**Demo Question:**  
`How are incidents escalated in the support process?`  

Expected output: Answer displayed in UI with source label (AI/HITL).  
*(Currently placeholder if UI fails.)*

`[Looks like the result wasn't safe to show. Let's switch things up and try something else!]`

---

## 🧪 Demo Queries
- `What is the SLA for Priority 1 incidents?` → *Target response time is 1 hour.*  
- `How are incidents escalated?` → *Escalated to 2nd/3rd line support.*  
- `What happens if maintenance isn’t renewed?` → *Support is discontinued.*  

---

## 🚀 Future Enhancements
- Multi-PDF support  
- Feedback loop for continuous improvement  
- Memory integration for personalization  
- Cloud deployment for scalability  

---

## 📸 Screenshots
All screenshots are stored in `/OutPut-Screenshots` folder and referenced in this README.  
